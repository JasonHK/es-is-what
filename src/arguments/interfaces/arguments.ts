/**
 * Representing the `arguments` object accessible inside functions that contains the values of the
 * arguments passed to that function.
 * 
 * @since 0.0.1
 */
export type IArguments = globalThis.IArguments;
