import type NumberOpaque from "./_helpers/NumberOpaque";
import type NumberType from "./_helpers/NumberType";

/**
 * Represents a number whose value is `-0`.
 * 
 * @category Number
 * @since 0.0.1
 */
type NegativeZero = NumberOpaque<-0, NumberType.NegativeZero>;

export default NegativeZero;
